Our project contains two parts. Report and C code. 
Please use dev c++ or other feasible compiler to compile before you evaluate our program.

Just make a new project and open main.c compile and run it.
We only provide file-input You can run the program by changing the number of example.txt and run exe by inputing "example.txt".£¨example.txt must in the same catalogue with BinarySearchTree.exe£©
The input form:
 *  1.A positive integer N.
 *  2.N lines each contains the left and the right children of a node in the format left_index right_index.
 *  3.N distinct integer keys.
You can see the result in the exe directly.
